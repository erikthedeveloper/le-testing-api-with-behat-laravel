default:
    extensions:
        Behat\WebApiExtension:
            base_url: http://behat-api.homestead/
    suites:
        api:
            paths:    [ %paths.base%/features/api ]
            contexts: [ ApiFeatureContext ]
